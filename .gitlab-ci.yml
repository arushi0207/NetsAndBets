image: demo/Dockerfile

stages:
  - build
  - test
  - deploy


build:
  stage: build
  script:
    - mvn clean install -DskipTests
  artifacts:
    paths:
      - target/*.jar
    expire_in: 1 day

test:
  stage: test
  script:
    - mvn test
  dependencies:
    - build